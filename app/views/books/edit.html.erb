<h1>Editing Book</h1>
   <% if @book.errors.any? %>
   <div class="errors-top">
  <%= @book.errors.count %> <strong>error prohibited this book from being saved:</strong></div>
     <div class="errors-bottom">
      <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li class="errors-bottom-font">・<%= message %></li>
      <% end %>
  </ul>
  </div>
 <% end %>
 <div class="box">
    <%= form_with model: @book do |f| %>
  <h4>title</h4>
    <%= f.text_field :title %>

  <h4>body</h4>
    <%= f.text_area :body %>
  <br>
    <%= f.submit 'Update Book' %>
  <% end %>
  <br>
    <div class="box-edit">
      <a href="/books/<%= @book.id %>">Show</a>|<a href="/books">Back</a></div>
</div>
